##This is for brand new arch installs that don't have this yet. You don't need to do this if you're running EndeavourOS, as it has Yay pre-installed.

pacman -S --needed git base-devel
git clone https://aur.archlinux.org/yay-bin.git
cd yay-bin && makepkg -si
